<% if @permitted %>
$("#course-form").html("<%= escape_javascript(render partial: 'form', locals: { course: @course }) %>");
$("input.date_picker").datepicker({
  dateFormat: 'yy-mm-dd'
});
$("#course-modal").modal('show');
<% else %>
alert('You do not have permissions to edit this entry');
<% end %>