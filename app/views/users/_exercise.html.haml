%tr
  %td= l exercise.date
  %td= exercise.repetitions
  %td= exercise.duration
  %td
    .btn-group
      = link_to 'Edit', edit_exercise_path(exercise), class: 'btn btn-secondary'
      = link_to 'Delete', exercise_path(exercise), method: :delete, class: 'btn btn-danger'
